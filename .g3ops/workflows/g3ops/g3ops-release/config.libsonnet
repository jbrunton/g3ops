{
  jobs: {
    // Note: if you publish draft releases then you'll need to provide a token with admin permissions here
    check_manifest: {
      token_name: "G3OPS_ADMIN_ACCESS_TOKEN"
    },
    release: {
      token_name: "GITHUB_TOKEN"
    }
  }
}
