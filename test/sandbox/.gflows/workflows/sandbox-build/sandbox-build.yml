name: sandbox-build
"on":
  workflow_run:
    workflows: ["sandbox-test"]
    branches: ["develop"]
    types:
      - completed

jobs:
  manifest-check:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Checking manifest!"
