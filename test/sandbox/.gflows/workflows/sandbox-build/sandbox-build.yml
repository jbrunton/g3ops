name: sandbox-build
"on":
  workflow_run:
    workflows: ["sandbox-test"]
    branches: ["g3ops-55", "develop"]
    types:
      - completed

jobs:
  manifest-check:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Checking manifest!"
